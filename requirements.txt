# CasePilot — requirements.txt
#
# Core: Python standard library (no external deps for the pipeline).
# UI:   Streamlit dashboard for demonstration (ui.py).

# Dashboard — launch with: streamlit run ui.py
streamlit>=1.35
